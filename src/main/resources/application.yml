server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/signalops
    username: signalops
    password: signalops
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

jwt:
  secret: super-secret-key-change-later
  expiration: 86400000
